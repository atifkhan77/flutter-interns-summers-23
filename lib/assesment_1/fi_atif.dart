import 'package:flutter/material.dart';

class FIAtif extends StatelessWidget {
  const FIAtif({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Atif"),
      ),
      body: const Center(
        child: Text("Hello! I am Atif"),
      ),
    );
  }
}
